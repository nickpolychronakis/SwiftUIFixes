import XCTest
@testable import SwiftUIFixes

final class SwiftUIFixesTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIFixes().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
